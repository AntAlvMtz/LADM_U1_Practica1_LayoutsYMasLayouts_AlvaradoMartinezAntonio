<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="mx.edu.ittepic.ladm_u1_practica1_layoutsymaslayouts">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.LADM_U1_Practica1_LayoutsYMasLayouts">
        <activity
            android:name=".Agregar_ruta"
            android:exported="false"
            android:label="@string/title_activity_agregar_ruta"
            android:theme="@style/Theme.LADM_U1_Practica1_LayoutsYMasLayouts.NoActionBar" />
        <activity
            android:name=".Mis_rutas"
            android:exported="false"
            android:label="@string/title_activity_mis_rutas" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>